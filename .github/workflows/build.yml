name: Build SurfOS Alpine ISO

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Docker the official way
      run: |
        sudo apt update
        sudo apt install -y ca-certificates curl gnupg lsb-release
        sudo mkdir -p /etc/apt/keyrings
        curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
        echo \
          "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
          $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
        sudo apt update
        sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

    - name: Start Docker service
      run: sudo systemctl start docker

    - name: Build SurfOS ISO inside Alpine container
      run: |
        sudo docker run --rm -v $(pwd):/workdir -w /workdir \
          alpine:latest sh -c "
            apk update &&
            apk add alpine-sdk alpine-make-vm-image git curl qemu qemu-img syslinux xorriso &&
            chmod +x mkimg.surfos.sh &&
            echo '=== Starting build ===' &&
            ./mkimg.surfos.sh --verbose &&
            echo '=== Build finished ===' &&
            ls -la . || echo 'No files in directory' &&
            find . -name '*.iso' -ls || echo 'No .iso files found anywhere'
          "

    - name: List all possible ISO files
      run: |
        ls -la *.iso *.img || echo "Ingen ISO/IMG fundet"
        find . -name '*.iso' -o -name '*.img' || echo "Ingen ISO/IMG fundet nogen steder"

    - name: Upload any ISO found
      uses: actions/upload-artifact@v4
      with:
        name: surfos-alpine-iso
        path: |
          *.iso
          *.img
        if-no-files-found: warn
